/**
 * Standalone Weapons View (/weapons) - Production Safe Placeholder
 */

export function renderWeaponsView(container) {
  if (!container) return;

  container.innerHTML = `
    <div class="data-page-wrapper">
      <div class="data-page-card">
        <h2 class="data-card-title">Vũ Khí</h2>
        <p class="data-card-subtext">Nội dung đang được demo trong tưởng tượng, vui lòng đợi.</p>
      </div>
    </div>
  `;
}
